# Read-only legacy Supabase database URL. Supply at runtime; never commit the real value.
SUPABASE_DB_URL=${SECRET_STORE_BINDING_REQUIRED}

# Required only for an explicitly approved --confirm-archive-write run.
R2_ENDPOINT_URL=${PROVIDER_ENDPOINT_CONFIGURED_EXTERNALLY}
R2_ACCESS_KEY_ID=${SECRET_STORE_BINDING_REQUIRED}
R2_SECRET_ACCESS_KEY=${SECRET_STORE_BINDING_REQUIRED}
