# CI trigger scope

The archive exporter validation workflow triggers only for changes under the archive tooling, archive documentation/templates, or the workflow itself. It performs validation/tests only and has no archive credentials.
