# Caroline runtime access to Infisical-backed secrets-gateway.
# Provider/database credentials do not belong in this file or in application Worker secrets.
GATEWAY_TOKEN=
GATEWAY_URL=

# Optional local Wrangler authentication only. Do not store these in project files.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
