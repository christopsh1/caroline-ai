# LOCAL DEVELOPMENT ONLY. Copy to .dev.vars outside source control.
# Caroline provider/database credentials stay in Infisical Development.
# Local Workers receive only the two secrets-gateway access values below.

GATEWAY_TOKEN=
GATEWAY_URL=
